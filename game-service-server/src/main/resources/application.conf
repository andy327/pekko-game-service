pekko-game-service {
  db {
    url = "jdbc:postgresql://localhost:5432/gamedb"
    url = ${?DATABASE_URL}
    user = "gameuser"
    user = ${?DB_USER}
    pass = "gamepass"
    pass = ${?DB_PASSWORD}
    poolSize = 8
  }

  http {
    host = "0.0.0.0"
    port = 8080
    port = ${?PORT}
  }
}

jwt {
  secret = "local-dev-secret"
  secret = ${?JWT_SECRET}
}
